# 🚀 ЛОКАЛЬНЫЙ ЗАПУСК - ПОЛНАЯ ИНСТРУКЦИЯ

## ⚡ БЫСТРЫЙ СТАРТ

```bash
npm run dev
```

**Готово!** Сервер запустится автоматически на свободном порту.

---

## 📋 КАК УЗНАТЬ ПОРТ?

Сервер **АВТОМАТИЧЕСКИ** выведет в консоль:

```
➜  Local:   http://localhost:5001/da-teens-webapp-tele/
```

**Просто открой этот URL в браузере!**

---

## ⚠️ ВАЖНО: ПОЧЕМУ НЕ ЛЮБОЙ ПОРТ?

**Твой проект** использует **GitHub Spark plugin** для KV-хранилища (`useKV`).

**Как работает:**
1. ✅ Пробует порт **5000** (Spark по умолчанию)
2. ✅ Если **5000 занят** → пробует **5001**
3. ✅ Если **5001 занят** → пробует **5002**
4. ✅ И так далее, пока не найдет **свободный**

**Почему нельзя выбрать порт вручную:**
- Spark plugin переопределяет конфигурацию Vite
- Он нужен для сохранения прогресса (`useKV` из `@github/spark/hooks`)
- Без него прогресс не сохраняется
- Это **не баг** - так работает Spark plugin

**Про Replit:** Файл `.replit` нужен ТОЛЬКО для Replit. Локально **не влияет**.

---

## 🔍 КАК ПРОВЕРИТЬ ЧТО РАБОТАЕТ?

### Вариант 1: Из логов
```bash
npm run dev > /tmp/vite.log 2>&1
tail /tmp/vite.log | grep "Local:"
```

### Вариант 2: Из скрипта
```bash
./scripts/check-local.sh
```

### Вариант 3: Из терминала
Просто посмотри в консоль где запускал `npm run dev` - там будет строка:
```
➜  Local:   http://localhost:ПОРТ/da-teens-webapp-tele/
```

---

## 🐛 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ?

### Шаг 1: Проверка статуса

```bash
# Проверить, запущен ли сервер
ps aux | grep -E "vite|node.*dev" | grep -v grep

# Проверить логи
tail -20 /tmp/vite*.log 2>/dev/null || echo "Логи не найдены"

# Проверить занятые порты
lsof -i :5001 -i :5000 -i :5173 -i :5174 | grep LISTEN
```

### Шаг 2: Быстрое восстановление

```bash
# 1. Убить старые процессы
pkill -9 -f vite

# 2. Очистить кэш
rm -rf node_modules/.vite

# 3. Запустить заново
npm run dev
```

### Шаг 3: Типичные проблемы

#### Проблема: Порт занят
**Решение:**
```bash
# Использовать скрипт диагностики
./scripts/fix-ports.sh

# Или освободить порт вручную
lsof -ti:5001 | xargs kill -9 2>/dev/null
```

#### Проблема: Белый экран / "Cannot GET"
**Решение:**
1. Проверить, запущен ли сервер: `ps aux | grep vite`
2. Запустить сервер: `npm run dev`
3. Проверить правильный URL: `http://localhost:ПОРТ/da-teens-webapp-tele/` (НЕ без `/da-teens-webapp-tele/`!)

#### Проблема: `ReferenceError: Chat is not defined`
**Решение:**
```bash
# 1. Проверить импорт в файле
grep "import.*Chat" src/app/App.tsx

# 2. Очистить кэш Vite
pkill -9 -f vite
rm -rf node_modules/.vite
npm run dev
```

---

## 📝 ПРАВИЛА ПОРТОВ

**⚡ ВАЖНО:** Порт автоматически выбирается из свободных!

**Этот проект:** `da-teens-webapp-tele`
- Dev server: **порт выбирается автоматически** (обычно 5000 или 5001)
- Preview: **порт 8080**
- `strictPort: false` - порт меняется автоматически если занят

**Другой проект:** `phuket-telegram-shop`
- Dev server: **порт 5000** или **8080**
- НЕ конфликтует с da-teens (Vite сам выберет свободный порт)

---

## ✅ ИТОГО

1. **Запускаешь:** `npm run dev`
2. **Смотришь:** На каком порту запустился (в консоли)
3. **Открываешь:** Этот URL в браузере
4. **Готово!** Работает!

**Больше ничего не нужно!**



## ⚡ БЫСТРЫЙ СТАРТ

```bash
npm run dev
```

**Готово!** Сервер запустится автоматически на свободном порту.

---

## 📋 КАК УЗНАТЬ ПОРТ?

Сервер **АВТОМАТИЧЕСКИ** выведет в консоль:

```
➜  Local:   http://localhost:5001/da-teens-webapp-tele/
```

**Просто открой этот URL в браузере!**

---

## ⚠️ ВАЖНО: ПОЧЕМУ НЕ ЛЮБОЙ ПОРТ?

**Твой проект** использует **GitHub Spark plugin** для KV-хранилища (`useKV`).

**Как работает:**
1. ✅ Пробует порт **5000** (Spark по умолчанию)
2. ✅ Если **5000 занят** → пробует **5001**
3. ✅ Если **5001 занят** → пробует **5002**
4. ✅ И так далее, пока не найдет **свободный**

**Почему нельзя выбрать порт вручную:**
- Spark plugin переопределяет конфигурацию Vite
- Он нужен для сохранения прогресса (`useKV` из `@github/spark/hooks`)
- Без него прогресс не сохраняется
- Это **не баг** - так работает Spark plugin

**Про Replit:** Файл `.replit` нужен ТОЛЬКО для Replit. Локально **не влияет**.

---

## 🔍 КАК ПРОВЕРИТЬ ЧТО РАБОТАЕТ?

### Вариант 1: Из логов
```bash
npm run dev > /tmp/vite.log 2>&1
tail /tmp/vite.log | grep "Local:"
```

### Вариант 2: Из скрипта
```bash
./scripts/check-local.sh
```

### Вариант 3: Из терминала
Просто посмотри в консоль где запускал `npm run dev` - там будет строка:
```
➜  Local:   http://localhost:ПОРТ/da-teens-webapp-tele/
```

---

## 🐛 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ?

### Шаг 1: Проверка статуса

```bash
# Проверить, запущен ли сервер
ps aux | grep -E "vite|node.*dev" | grep -v grep

# Проверить логи
tail -20 /tmp/vite*.log 2>/dev/null || echo "Логи не найдены"

# Проверить занятые порты
lsof -i :5001 -i :5000 -i :5173 -i :5174 | grep LISTEN
```

### Шаг 2: Быстрое восстановление

```bash
# 1. Убить старые процессы
pkill -9 -f vite

# 2. Очистить кэш
rm -rf node_modules/.vite

# 3. Запустить заново
npm run dev
```

### Шаг 3: Типичные проблемы

#### Проблема: Порт занят
**Решение:**
```bash
# Использовать скрипт диагностики
./scripts/fix-ports.sh

# Или освободить порт вручную
lsof -ti:5001 | xargs kill -9 2>/dev/null
```

#### Проблема: Белый экран / "Cannot GET"
**Решение:**
1. Проверить, запущен ли сервер: `ps aux | grep vite`
2. Запустить сервер: `npm run dev`
3. Проверить правильный URL: `http://localhost:ПОРТ/da-teens-webapp-tele/` (НЕ без `/da-teens-webapp-tele/`!)

#### Проблема: `ReferenceError: Chat is not defined`
**Решение:**
```bash
# 1. Проверить импорт в файле
grep "import.*Chat" src/app/App.tsx

# 2. Очистить кэш Vite
pkill -9 -f vite
rm -rf node_modules/.vite
npm run dev
```

---

## 📝 ПРАВИЛА ПОРТОВ

**⚡ ВАЖНО:** Порт автоматически выбирается из свободных!

**Этот проект:** `da-teens-webapp-tele`
- Dev server: **порт выбирается автоматически** (обычно 5000 или 5001)
- Preview: **порт 8080**
- `strictPort: false` - порт меняется автоматически если занят

**Другой проект:** `phuket-telegram-shop`
- Dev server: **порт 5000** или **8080**
- НЕ конфликтует с da-teens (Vite сам выберет свободный порт)

---

## ✅ ИТОГО

1. **Запускаешь:** `npm run dev`
2. **Смотришь:** На каком порту запустился (в консоли)
3. **Открываешь:** Этот URL в браузере
4. **Готово!** Работает!

**Больше ничего не нужно!**







